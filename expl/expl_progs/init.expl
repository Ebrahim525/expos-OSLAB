int main()
{
    decl
        int temp;
        string w, fileName;
    enddecl
    begin
        w = "Enter File: ";
        temp = exposcall("Write", -2, w);
        temp = exposcall("Read", -1, fileName);
        
        temp = exposcall("Exec", fileName);

        return 0;
    end
}