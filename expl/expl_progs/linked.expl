type
    linkedlist {
        int key;
        linkedlist next;
    }
endtype

int main()
{
    decl
        int n, temp, i, a;
        string w;
        linkedlist head, curr, temp2;
    enddecl
    begin
        w = "Enter N: ";
        temp = exposcall("Write", -2, w);
        temp = exposcall("Read", -1, n);

        temp = exposcall ("Heapset");
        
        temp = exposcall("Read", -1, a);
        breakpoint;
        head = exposcall ("Alloc",  8);
        breakpoint;
        head.key = a;
        head.next = null;
        curr = head;

        temp = exposcall("Read", -1, a);
        breakpoint;
        temp2 = exposcall ("Alloc",  8);
        breakpoint;
        temp2.key = a;
        temp2.next = null;
        curr.next = temp2;
        curr = temp2;
        
        /*
        i = 1;
        while(i < n) do
            breakpoint;
            temp = exposcall("Read", -1, a);
            breakpoint;
            temp2 = exposcall ("Alloc",  8);
            breakpoint;
            temp2.key = a;
            temp2.next = null;

            curr.next = temp2;
            curr = temp2;

            breakpoint;
            i = i + 1;
        endwhile;
        
        // Print
        curr = head;
        while(curr != null) do
            temp = exposcall("Write", -2, curr.key);

            curr = curr.next;
        endwhile;


        // Free
        curr = head;
        while(curr != null) do
            head = head.next;
            temp = exposcall ("Free", curr);

            curr = head;
        endwhile;
        */
        return 0;
    end
}